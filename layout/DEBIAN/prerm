#!/bin/sh
set -e

PREFS=/var/mobile/Library/Preferences/com.openminis.app.plist
if [ -f "$PREFS" ]; then
    plutil -remove mtl_enabled "$PREFS" 2>/dev/null || true
    plutil -remove mtl_frame   "$PREFS" 2>/dev/null || true
    plutil -remove mtl_lock    "$PREFS" 2>/dev/null || true
fi

exit 0
